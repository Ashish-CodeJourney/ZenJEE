"use client";

// MindfulnessView — stub (Phase 3 expands)
export default function MindfulnessView() {
  return (
    <section aria-labelledby="mindfulness-heading">
      <h2 id="mindfulness-heading" className="text-xl font-semibold text-slate-800 mb-4">
        Mindfulness Exercises
      </h2>
      <p className="text-slate-500">Mindfulness exercises coming in Phase 3…</p>
    </section>
  );
}
